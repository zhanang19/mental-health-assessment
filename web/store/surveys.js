import { state } from './submodules/surveys/surveys.state'
import { actions } from './submodules/surveys/surveys.actions'
import { mutations } from './submodules/surveys/surveys.mutations'
import { getters } from './submodules/surveys/surveys.getters'

export default {
  state,
  getters,
  mutations,
  actions
}
