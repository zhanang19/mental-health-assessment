import { state } from "./submodules/survey-responses/survey-responses.state";
import { actions } from "./submodules/survey-responses/survey-responses.actions";
import { mutations } from "./submodules/survey-responses/survey-responses.mutations";
import { getters } from "./submodules/survey-responses/survey-responses.getters";

export default {
  state,
  getters,
  mutations,
  actions
};
